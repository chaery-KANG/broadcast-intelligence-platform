# Run these commands in your project folder:

mkdir -p src docs data experiments

# Copy all files from this pack into your repository root.

# Then run:
git add README.md src/featureExtractor.js src/temporalAnomalyDetector.js data/sample-archive-days.json docs/model-card.md experiments/001-baseline-anomaly-report.md
git commit -m "Add temporal anomaly detection feature pipeline"
git push origin main
